env_config: {
  env_type: 'othello',
  board_size: 8
}
demo_config: {
  evaluator1_config: {
    algo_config: {
      name: 'greedy_mcts',
      heuristic: 'num_tiles',
      num_iters: 100,
      max_nodes: 100,
      puct_coeff: 1.0,
      dirichlet_alpha: 0.1,
      dirichlet_epsilon: 0.0
    }
  },
  evaluator2_config: {
    algo_config: {
      name: 'human'
    }
  },
  manual_step: False
}